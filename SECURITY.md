# 安全策略

## 报告安全问题

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交漏洞。不要在公开 Issue、
讨论区或日志附件中披露可利用细节、API Key、私人媒体和完整本机路径。

报告尽量包含：

- 受影响的版本或 commit；
- 操作系统和安装方式；
- 最小复现步骤；
- 实际影响和攻击者需要具备的条件；
- 已去除个人数据的证明材料；
- 你是否已经向相关上游组件报告。

不要为了证明问题而访问他人的系统、媒体、密钥或云账户。维护者确认修复和用户缓解措施后，
再协调公开时间。

## 支持范围

安全修复面向当前默认分支和最新正式发行包。报告问题前请确认它仍能在当前代码或最新发行包
复现；第三方模型、运行时和云服务的漏洞也应同时报告给相应上游。

## 安全边界

ASMR Dubber 按单用户本地工具设计：

- 网页默认只监听 `127.0.0.1`；
- 程序目录默认由当前操作系统账户独占；
- 模型、项目和缓存视为本地受信数据；
- 翻译和外部 TTS（语音合成）服务是独立的网络信任边界；
- 安装器会执行经过固定版本和哈希校验的第三方代码。

它不是多租户服务，也不是面向公网的媒体处理平台。把网页暴露给不受信用户、让多人共享同一
程序目录，或以高权限账户处理不受信文件，会超出默认威胁模型。

## 便携式明文密钥

API Key 明文保存在：

```text
.asmr-dubber/config/secrets.json
```

这是明确的便携性取舍：复制或删除整个程序文件夹时，密钥与其它数据一起移动或删除。程序不
声称该文件经过加密，也不依赖系统凭据库。

用户应当：

- 用操作系统账户权限保护程序目录；
- 不共享 `.asmr-dubber`，不把它提交到 Git 或公开云盘；
- 不把 `secrets.json`、完整配置目录或项目打包进问题附件；
- 为不同服务使用权限最小、额度可控的独立密钥；
- 怀疑泄露时立即到供应商处撤销并轮换；
- 删除目录前关闭程序和相关模型服务，确认没有同步或备份副本。

密钥不得进入 `project.json`、`performance.json`、Setup 日志、字幕、导出表格或网页可下载
文件。发现这类泄露属于应私下报告的安全问题。

## 网页访问

- loopback 监听不启用登录，依赖本机账户边界；
- 绑定 `0.0.0.0` 或其它非 loopback 地址时强制用户名和密码；
- 未提供密码时，程序为本次进程生成随机密码并打印在启动终端；
- 不启用 Gradio 公共分享隧道；
- 网页只允许读取 `.asmr-dubber/temp/ui` 下经过暂存的输出；
- 默认上传上限为 20 GB。

即使启用了登录，也不要把开发用网页直接暴露到公网。远程使用应放在受控内网或经过正确配置
的 HTTPS 反向代理之后，并限制来源地址。启动终端中的随机密码也属于敏感信息。

## 媒体和项目文件

上传的媒体可能触发 PyAV、FFmpeg、音频编解码器和字幕解析器。只处理可信来源，尤其不要以
管理员/root 身份运行。最终输出和项目相对路径会经过目录边界检查；路径逃逸、符号链接绕过或
让网页读取任意本机文件都应作为安全漏洞报告。

项目 manifest 使用严格 schema 和 revision。不要手工关闭验证、修改哈希或把来自陌生人的
完整项目直接放进包含私人数据的程序目录。

## 模型和运行时供应链

安装器按 `mirrors.json` 选择来源，并对固定制品检查路径、字节数和 SHA-256。模型包还会验证
内部 manifest 和每个文件。海外下载源默认关闭。

这些检查能发现传输损坏和未经合同更新的替换，但不能证明上游代码或模型没有漏洞。维护者应：

- 固定上游 revision；
- 把已经发布的 ModelScope 路径视为不可变；
- 审查源码归档和依赖锁；
- 保留许可证和 notices；
- 在隔离、低权限环境中构建依赖包；
- 不接受来源不明的模型 ZIP、wheelhouse 或 EXE。

用户不应通过编辑 manifest、关闭哈希检查或替换同名 DLL 来“修复”安装。

## 外部服务和隐私

根据配置，翻译服务会收到识别文字，多 ASR（语音识别）校对服务会收到多个候选和用户填写的
场景背景，外部 TTS 服务会收到中文、参考音频或本地路径。程序不能控制服务端如何记录、训练
或再处理这些数据。

发送前检查供应商的数据政策、账号权限和地区要求。敏感作品可以选择本地识别、本地 TTS 和
用户自行管理的本地翻译接口，仍需确认所用模型和服务端本身可信。

## 日志和问题附件

Setup 日志主要记录下载、路径、依赖和错误。虽然代码避免打印密钥，第三方异常仍可能包含请求
地址、用户名或媒体文件名。公开前应人工检查并脱敏。

建议只提供与复现有关的片段，不要上传：

- `.asmr-dubber/config`；
- 完整环境变量列表；
- HTTP Authorization 头；
- 私人项目和参考音频；
- 带用户名的完整目录树；
- 云服务响应正文中的账户信息。

## 内容使用风险

模型误识别、翻译错误、音色相似度和生成内容权利通常不是软件安全漏洞，但可能造成真实伤害。
使用者必须获得处理作品和声音所需的授权，不得用本工具冒充、欺诈、骚扰或侵犯隐私，并应按
适用要求标记合成内容。

许可证和权利边界见[第三方软件、模型与服务](docs/THIRD_PARTY_NOTICES.md)。
